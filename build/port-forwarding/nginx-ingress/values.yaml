image:
  # image.repositoryUrl must be supplied via helm install
  repositoryUrl: vsclkonlinedev.azurecr.io
  defaultBackendName: 'k8s-defaultbackend'
  defaultBackendTag: '1.4'
  ingressControllerName: 'nginx-ingress-controller'
  ingressControllerTag: '0.27.1'
ingress:
  replicaCount: 1
# This is used for nginx to set hte x-servedby header
# specified on the helm upgrade command-line
clusterName:
