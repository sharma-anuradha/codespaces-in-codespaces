.create-or-alter function
with (docstring = 'Breakdown of all the responses, excluding 307.', folder='Dashboards/Reliability/Create')
getCreateResponseBreakdown
(T:(EventAtCreateRequestStop_ResponseCode: int, TimeStamp: datetime, ResponseOutcome: string))
{
    T
    | where EventAtCreateRequestStop_ResponseCode != 307
    | where TimeStamp < ago(1h)
    | summarize count() by ResponseOutcome
}
