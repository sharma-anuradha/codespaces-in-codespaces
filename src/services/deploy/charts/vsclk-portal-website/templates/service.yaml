apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsclk-portal-website.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "vsclk-portal-website.name" . }}
    helm.sh/chart: {{ include "vsclk-portal-website.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "vsclk-portal-website.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
