apiVersion: v1
kind: Service
metadata:
  name: {{ template "vsclk-portal-website.fullname" . }}
  labels:
    app: {{ template "vsclk-portal-website.name" . }}
    chart: {{ template "vsclk-portal-website.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "vsclk-portal-website.name" . }}
    release: {{ .Release.Name }}
