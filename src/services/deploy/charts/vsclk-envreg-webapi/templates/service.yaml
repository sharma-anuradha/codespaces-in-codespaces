apiVersion: v1
kind: Service
metadata:
  name: {{ include "vsclk-envreg-webapi.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "vsclk-envreg-webapi.name" . }}
    helm.sh/chart: {{ include "vsclk-envreg-webapi.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "vsclk-envreg-webapi.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
