apiVersion: v1
kind: Service
metadata:
  name: {{ template "vsclk-envreg-webapi.fullname" . }}
  labels:
    app: {{ template "vsclk-envreg-webapi.name" . }}
    chart: {{ template "vsclk-envreg-webapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "vsclk-envreg-webapi.name" . }}
    release: {{ .Release.Name }}
