{
    "private": true,
    "scripts": {
        "build:pack-publish": "cd signalr-client-proxy && yarn build:pack-publish && cd ../signalr-client && yarn build:pack-publish && cd ../jsonrpc  && yarn build:pack-publish"
    },
    "devDependencies": {
        "tslint": "^5.13.1",
        "tslint-microsoft-contrib": "^6.0.0",
        "typescript": "^3.7.4",
        "gulp": "^4.0.2",
        "nerdbank-gitversioning": "^2.3.183"
    },
    "workspaces": [
        "./signalr-client-proxy",
        "./signalr-client",
        "./jsonrpc",
        "./cli"
    ]
  }